#bed mesh configuration for D1000Pro

[bed_mesh]
speed: 300
horizontal_move_z: 2
mesh_min: 35, 30
mesh_max: 1050, 1050
probe_count: 50,50
algorithm: bicubic
bicubic_tension: 0.2
mesh_pps: 4, 4
fade_start: 1.0
fade_end: 30.0
scan_overshoot: 8

########################################
# Adaptive mesh
########################################

[gcode_macro Adaptive_Mesh]
description: This macro contains all adjustable settings for KAMP
variable_verbose_enable: True               # Set to True to enable KAMP information output when running. This is useful for debugging.
variable_mesh_margin: 5                     # Expands the mesh size in millimeters if desired. Leave at 0 to disable.
variable_fuzz_amount: 0                     # Slightly randomizes mesh points to spread out wear from nozzle-based probes. Leave at 0 to disable.

gcode: # Gcode section left intentionally blank. Do not disturb.
#   Noting

[gcode_macro BED_MESH_CALIBRATE]
rename_existing: BED_MESH_CALIBRATE_ORIG
gcode:
    SET_FAN_FULL_SPEED ENABLE=1
    M118 Waiting for eddy temperature to stabilize...
    PROBE_EDDY_NG_SETUP
    BED_MESH_CALIBRATE_ORIG {rawparams}
    SET_FAN_FULL_SPEED ENABLE=0
